name: Docker CI

on:
  push:
    branches:
      - main:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: Build Docker image
        run: docker build -t myapp2:v1 ./appbox/
