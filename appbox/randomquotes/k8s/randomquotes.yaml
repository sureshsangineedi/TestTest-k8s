apiVersion: apps/v1
kind: Deployment
metadata:
  name: randomquotes-app
  labels:
    app: randomquotes-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: randomquotes-app
  template:
    metadata:
      labels:
        app: randomquotes-app
    spec:
      containers:
      - name: randomquotes-app
        image: suresh2dev2/random-quotes:latest  # replace with your image
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: randomquotes-app-service
spec:
  selector:
    app: randomquotes-app
  type: NodePort   # For external access in most clusters
  ports:
  - protocol: TCP
    port: 8080         # External service port
    targetPort: 80     # Container port inside the pod
    nodePort: 30019    # Optional: specify NodePort (30000–32767 range)
