apiVersion: v1
kind: Service
metadata:
  name: redis-demo-app-service
spec:
  selector:
    app: redis-demo-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000
  type: NodePort
